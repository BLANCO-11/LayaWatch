/* Skeleton manifest entry: Skeleton and RouteProgress live in EmptyState.tsx
 * next to the other loading-state helpers (section 4.25). */
export { Skeleton, RouteProgress, default } from "./EmptyState";
