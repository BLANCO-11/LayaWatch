/* Chart chrome manifest entry: ChartCard lives in components/charts next to
 * the SVG primitives (section 4.14). Re-exported for the catalog. */
export { default } from "@/components/charts/ChartCard";
