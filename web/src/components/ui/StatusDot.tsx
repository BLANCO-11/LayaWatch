/* StatusDot manifest entry: StatusDot lives in components/shell next to the
 * LivePill that renders it (sections 4.32 and 7). Re-exported for the catalog. */
export { default } from "@/components/shell/StatusDot";
export type { DotTone } from "@/components/shell/StatusDot";
