/* LivePill manifest entry: LivePill lives in components/shell next to the
 * Topbar that renders it (sections 4.32 and 7). Re-exported for the catalog. */
export { default } from "@/components/shell/LivePill";
