/* Drawer manifest entry: the Drawer implementation lives in Dialog.tsx
 * next to the shared focus-trap helper and identical Escape rules. */
export { Drawer } from "./Dialog";
export { default } from "./Dialog";
